apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-backend
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "rybbit.labels" . | nindent 4 }}
    app.kubernetes.io/component: backend
  annotations:
    rollme: {{ .Values.backend.resources | toJson | sha256sum }}
spec:
  replicas: {{ .Values.backend.replicaCount }}
  selector:
    matchLabels:
      {{- include "rybbit.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: backend
  template:
    metadata:
      labels:
        {{- include "rybbit.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: backend
    spec:
      {{- with .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      securityContext:
        {{- with .Values.global.podSecurityContext }}
        {{- if .enabled }}
        fsGroup: {{ .fsGroup }}
        {{- end }}
        {{- end }}
      initContainers:
        - name: wait-for-valkey
          image: docker.io/valkey/valkey:9.1.1-alpine
          imagePullPolicy: IfNotPresent
          env:
            - name: VALKEY_HOST
              {{- if hasKey .Values.backend.env "REDIS_HOST" }}
              value: {{ .Values.backend.env.REDIS_HOST | quote }}
              {{- else if .Values.valkey.enabled }}
              value: {{ printf "%s-valkey.%s.svc.cluster.local" .Release.Name .Release.Namespace | quote }}
              {{- else }}
              value: "localhost"
              {{- end }}
            - name: VALKEY_PORT
              {{- if hasKey .Values.backend.env "REDIS_PORT" }}
              value: {{ .Values.backend.env.REDIS_PORT | quote }}
              {{- else }}
              value: "6379"
              {{- end }}
            - name: VALKEY_PASSWORD
              {{- if hasKey .Values.backend.env "REDIS_PASSWORD" }}
              value: {{ .Values.backend.env.REDIS_PASSWORD | quote }}
              {{- else if .Values.valkey.enabled }}
              valueFrom:
                secretKeyRef:
                  name: {{ printf "%s-valkey-auth" .Release.Name }}
                  key: default-password
              {{- else }}
              value: ""
              {{- end }}
          command:
            - sh
            - -c
            - |
              echo "Waiting for Valkey to be ready..."
              until valkey-cli -h "${VALKEY_HOST}" -p "${VALKEY_PORT}" -a "${VALKEY_PASSWORD}" --no-auth-warning ping | grep -q PONG; do
                echo "Valkey is not ready yet..."
                sleep 2
              done
              echo "Valkey is ready!"
        - name: wait-for-clickhouse
          image: busybox:1.35
          env:
            - name: CLICKHOUSE_HOST
              {{- if hasKey .Values.backend.env "CLICKHOUSE_HOST" }}
              value: {{ .Values.backend.env.CLICKHOUSE_HOST | quote }}
              {{- else }}
              value: {{ printf "http://clickhouse-%s-clickhouse.%s.svc.cluster.local:8123" .Release.Name .Release.Namespace | quote }}
              {{- end }}
            {{- if hasKey .Values.backend.env "SKIP_CLICKHOUSE_WAIT" }}
            - name: SKIP_CLICKHOUSE_WAIT
              value: {{ .Values.backend.env.SKIP_CLICKHOUSE_WAIT | quote }}
            {{- end }}
          command:
            - sh
            - -c
            - |
              if [ "${SKIP_CLICKHOUSE_WAIT}" = "true" ]; then
                echo "SKIP_CLICKHOUSE_WAIT=true; skipping ClickHouse readiness check."
                exit 0
              fi
              echo "Waiting for ClickHouse to be ready..."
              until wget -q --spider "${CLICKHOUSE_HOST}/ping"; do
                echo "ClickHouse is not ready yet..."
                sleep 2
              done
              echo "ClickHouse is ready!"
        - name: wait-for-postgres
          image: docker.io/postgres:15-alpine
          env:
            - name: PGHOST
              {{- if hasKey .Values.backend.env "POSTGRES_HOST" }}
              value: {{ .Values.backend.env.POSTGRES_HOST | quote }}
              {{- else }}
              value: {{ printf "%s-postgresql-rw.%s.svc.cluster.local" .Release.Name .Release.Namespace | quote }}
              {{- end }}
            - name: PGPORT
              {{- if hasKey .Values.backend.env "POSTGRES_PORT" }}
              value: {{ .Values.backend.env.POSTGRES_PORT | quote }}
              {{- else }}
              value: "5432"
              {{- end }}
          command:
            - sh
            - -c
            - |
              echo "Waiting for PostgreSQL to be ready..."
              until pg_isready; do
                echo "PostgreSQL is not ready yet..."
                sleep 2
              done
              echo "PostgreSQL is ready!"
      {{- with .Values.global.podAntiAffinity }}
      affinity:
        podAntiAffinity:
          {{- if eq . "hard" }}
          requiredDuringSchedulingIgnoredDuringExecution:
            - labelSelector:
                matchLabels:
                  app.kubernetes.io/component: backend
              topologyKey: kubernetes.io/hostname
          {{- else }}
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchLabels:
                    app.kubernetes.io/component: backend
                topologyKey: kubernetes.io/hostname
          {{- end }}
      {{- end }}
      containers:
        - name: backend
          image: "{{ .Values.backend.image.repository }}:{{ .Values.backend.image.tag }}"
          imagePullPolicy: {{ .Values.backend.image.pullPolicy }}
          securityContext:
            {{- with .Values.global.containerSecurityContext }}
            {{- if .enabled }}
            runAsUser: 1001
            runAsNonRoot: true
            allowPrivilegeEscalation: {{ .allowPrivilegeEscalation }}
            {{- end }}
            {{- end }}
          resources:
            {{- toYaml .Values.backend.resources | nindent 12 }}
          env:
            {{- /* Add user-provided environment variables first */ -}}
            {{- range $key, $value := .Values.backend.env }}
            - name: {{ $key }}
              value: {{ $value | quote }}
            {{- end }}
            {{- /* Add default database configuration only if not provided by user */ -}}
            {{- if not (hasKey .Values.backend.env "CLICKHOUSE_HOST") }}
            - name: CLICKHOUSE_HOST
              value: {{ printf "http://clickhouse-%s-clickhouse.%s.svc.cluster.local:8123" .Release.Name .Release.Namespace | quote }}
            {{- end }}
            {{- if not (hasKey .Values.backend.env "CLICKHOUSE_USER") }}
            - name: CLICKHOUSE_USER
              value: {{ .Values.clickhouse.auth.username | default "default" | quote }}
            {{- end }}

            {{- if not (hasKey .Values.backend.env "CLICKHOUSE_PASSWORD") }}
            - name: CLICKHOUSE_PASSWORD
              {{- if .Values.clickhouse.auth.existingSecret }}
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.clickhouse.auth.existingSecret }}
                  key: {{ .Values.clickhouse.auth.existingSecretKey | default "password" }}
              {{- else if .Values.clickhouse.clickhouse.defaultUser.password }}
              valueFrom:
                secretKeyRef:
                  name: {{ .Release.Name }}-clickhouse-credentials
                  key: password
              {{- else }}
              valueFrom:
                secretKeyRef:
                  name: {{ .Release.Name }}-clickhouse-password
                  key: password
              {{- end }}
            {{- end }}
            {{- if not (hasKey .Values.backend.env "POSTGRES_HOST") }}
            - name: POSTGRES_HOST
              value: {{ .Release.Name }}-postgresql-rw.{{ .Release.Namespace }}.svc.cluster.local
            {{- end }}
            {{- if not (hasKey .Values.backend.env "POSTGRES_PORT") }}
            - name: POSTGRES_PORT
              valueFrom:
                secretKeyRef:
                  name: {{ .Release.Name }}-postgresql-app
                  key: port
            {{- end }}
            {{- if not (hasKey .Values.backend.env "POSTGRES_DB") }}
            - name: POSTGRES_DB
              valueFrom:
                secretKeyRef:
                  name: {{ .Release.Name }}-postgresql-app
                  key: dbname
            {{- end }}
            {{- if not (hasKey .Values.backend.env "POSTGRES_USER") }}
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: {{ .Release.Name }}-postgresql-superuser
                  key: user
            {{- end }}
            {{- /* Add secrets only if using built-in databases, or an externally-managed one via existingSecret */ -}}
            {{- if not (hasKey .Values.backend.env "POSTGRES_PASSWORD") }}
            {{- if .Values.postgresql.auth.existingSecret }}
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.postgresql.auth.existingSecret }}
                  key: {{ .Values.postgresql.auth.existingSecretKey | default "password" }}
            {{- else if .Values.postgresql.enabled }}
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ printf "%s-postgresql-superuser" .Release.Name }}
                  key: password
            {{- end }}
            {{- end }}

            {{- /* Valkey/Redis configuration */ -}}
            {{- if not (hasKey .Values.backend.env "REDIS_HOST") }}
            - name: REDIS_HOST
              {{- if .Values.valkey.enabled }}
              value: {{ printf "%s-valkey.%s.svc.cluster.local" .Release.Name .Release.Namespace | quote }}
              {{- else }}
              value: "localhost"
              {{- end }}
            {{- end }}
            {{- if not (hasKey .Values.backend.env "REDIS_PORT") }}
            - name: REDIS_PORT
              value: "6379"
            {{- end }}
            {{- if not (hasKey .Values.backend.env "REDIS_PASSWORD") }}
            - name: REDIS_PASSWORD
              {{- if .Values.valkey.enabled }}
              valueFrom:
                secretKeyRef:
                  name: {{ printf "%s-valkey-auth" .Release.Name }}
                  key: default-password
              {{- else }}
              value: ""
              {{- end }}
            {{- end }}

            {{- /* Add auth secret only if not provided by user */ -}}
            {{- if not (hasKey .Values.backend.env "BETTER_AUTH_SECRET") }}
            {{- if .Values.backend.betterSecret.secretName }}
            - name: BETTER_AUTH_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.backend.betterSecret.secretName }}
                  key: {{ .Values.backend.betterSecret.authKey }}
            {{- else }}
            - name: BETTER_AUTH_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ .Release.Name }}-better-auth
                  key: {{ .Values.backend.betterSecret.authKey | default "better-auth-secret" }}
            {{- end }}
            {{- end }}

            {{- /* MAPBOX_TOKEN is fully optional (globe viz falls back to a flat map without it) — only added if the user supplied it directly or configured a secret */ -}}
            {{- if not (hasKey .Values.backend.env "MAPBOX_TOKEN") }}
            {{- if .Values.backend.mapboxSecret.secretName }}
            - name: MAPBOX_TOKEN
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.backend.mapboxSecret.secretName }}
                  key: {{ .Values.backend.mapboxSecret.tokenKey | default "mapbox-token" }}
            {{- end }}
            {{- end }}
          ports:
            - name: http
              containerPort: {{ .Values.backend.service.targetPort }}
              protocol: TCP
          {{- with .Values.backend.livenessProbe }}
          {{- if .enabled }}
          livenessProbe:
            {{- omit . "enabled" | toYaml | nindent 12 }}
          {{- end }}
          {{- end }}
          {{- with .Values.backend.readinessProbe }}
          {{- if .enabled }}
          readinessProbe:
            {{- omit . "enabled" | toYaml | nindent 12 }}
          {{- end }}
          {{- end }}
          {{- with .Values.backend.startupProbe }}
          {{- if .enabled }}
          startupProbe:
            {{- omit . "enabled" | toYaml | nindent 12 }}
          {{- end }}
          {{- end }}
